services:
  bot:
    build: .
    container_name: mealtogether_bot
    env_file:
      - .env
    volumes:
      # ホストのsrcディレクトリをコンテナ内にマウント（開発時のホットリロード用）
      - ./src:/app/src
    init: true  # 【重要】PID 1問題を解決し、シグナルを正しく処理する
    restart: unless-stopped # クラッシュ時に自動再起動